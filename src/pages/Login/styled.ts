import { Box, styled } from "@mui/material";

export const BoxContainer = styled(Box)({
  background: "#ffffff",
  display: "flex",
  flexDirection: "column",
  gap: "16px",
  alignItems: "center",
  justifyContent: "center",
});

export const HeaderTitle = styled(Box)({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
});

export const MainBody = styled(Box)({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  gap: "16px",
});

export const MainInput = styled(Box)({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  gap: "12px",
});

export const BotMain = styled(Box)({
  display: "flex",
  flexDirection: "column",
  gap: "16px",
});

export const SupText = styled(Box)({
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
  marginTop: "12px",
});
